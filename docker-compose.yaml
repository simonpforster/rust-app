version: '3.9'
name: "rust-app"

services:

  service:
    build:
      context: .
      dockerfile: service/Dockerfile
    image: localhost/service:latest
    environment:
      - ENVIRONMENT=docker
    ports:
      - "8080:8080"

  stub:
    build: stub/.
    ports:
    - "8081:8080"